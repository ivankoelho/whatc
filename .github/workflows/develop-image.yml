name: Build develop image

on:
  push:
    branches:
      - main

jobs:
  develop-image:
    # Publishing the develop image pushes to the upstream author's Docker Hub
    # namespace (shridh0r/*), so it only makes sense on the canonical repo.
    # Forks lack the DOCKERHUB_* secrets and push rights; skip there so a fork's
    # main CI stays green (a skipped job reports neutral, not failure).
    if: ${{ github.event.repository.fork == false }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push develop image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            shridh0r/whatomate:develop
            ghcr.io/${{ github.repository_owner }}/whatomate:develop
          cache-from: type=gha
          cache-to: type=gha,mode=max
