# Crowdin Configuration
# Documentation: https://developer.crowdin.com/configuration-file/

project_id_env: CROWDIN_PROJECT_ID
api_token_env: CROWDIN_PERSONAL_TOKEN

# Preserve hierarchy of files and folders
preserve_hierarchy: true

files:
  # Source English file
  - source: /src/i18n/locales/en.json
    # Use the full locale code so Brazilian Portuguese resolves to
    # pt-BR.json (the app's locale filename), not pt.json.
    translation: /src/i18n/locales/%locale%.json
    # Update strings instead of replacing
    update_option: update_as_unapproved
